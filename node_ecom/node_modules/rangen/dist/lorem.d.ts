/**
 * TODO:
 * 1) fix quality: same words one after another etc.
 * 2) add params to make more flexible
 */
export declare const lorem: (params?: number | LoremParams) => string[];
export interface LoremParams {
    words: number;
    sentences: number;
}
