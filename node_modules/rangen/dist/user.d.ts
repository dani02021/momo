export declare const user: (params?: UserParams) => User[];
export interface User {
    gender: string;
    name: {
        title: string;
        first: string;
        last: string;
    };
    email: string;
    age: number;
    dob: Date;
    registered: Date;
    phone: string;
    id: string;
}
export interface UserParams {
    count?: number;
    gender?: 'male' | 'female' | 'any';
    minAge?: number;
    maxAge?: number;
}
