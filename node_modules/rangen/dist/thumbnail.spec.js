"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var thumbnail_1 = require("./thumbnail");
describe('thumbnail', function () {
    it('should return string', function () {
        expect(typeof thumbnail_1.thumb()).toEqual('string');
    });
    it('should create canvas', function () {
        var div = document.createElement('div');
        div.setAttribute('id', 'canvas');
        document.querySelector('body').appendChild(div);
        expect(document.querySelector('#canvas canvas')).toEqual(null);
        thumbnail_1.thumb({ selector: '#canvas' });
        expect(document.querySelector('#canvas canvas').nodeName).toEqual('CANVAS');
    });
    it('should create svg', function () {
        var div = document.createElement('div');
        div.setAttribute('id', 'svg');
        document.querySelector('body').appendChild(div);
        expect(document.querySelector('#svg svg')).toEqual(null);
        thumbnail_1.thumb({ selector: '#svg', svg: true });
        expect(document.querySelector('#svg svg').nodeName).toEqual('svg');
    });
    it('should fail if wrong selector provided', function () {
        expect(function () { return thumbnail_1.thumb({ selector: '#test' }); }).toThrowError();
    });
});
//# sourceMappingURL=thumbnail.spec.js.map